# AlgoSkill code release dependencies
# Tested with Python 3.10-3.12

# HTTP + utilities
requests>=2.31.0
numpy>=1.24.0

# LLM backends are called through provider-agnostic HTTP adapters in
# src/llm_client.py. Add provider-specific SDKs locally only if you extend the
# client with custom adapters.
